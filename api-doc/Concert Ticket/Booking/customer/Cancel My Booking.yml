info:
  name: Cancel My Booking
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/bookings/{{bookingId}}/cancel"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
