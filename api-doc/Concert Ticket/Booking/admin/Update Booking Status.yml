info:
  name: Update Booking Status
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/bookings/{{bookingId}}/status"
  body:
    type: json
    data: |-
      {
          "status": "failed"
      }
  auth: inherit

runtime:
  variables:
    - name: bookingId
      value: 37d07444-21ef-4301-a81c-b773ab6b9a79

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
