info:
  name: Get Booking By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/bookings/{{bookingId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
