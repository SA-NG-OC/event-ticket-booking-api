info:
  name: Get Voucher By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/vouchers/{{voucherId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
