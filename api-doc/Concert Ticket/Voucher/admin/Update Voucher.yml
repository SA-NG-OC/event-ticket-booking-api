info:
  name: Update Voucher
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/vouchers/{{voucherId}}"
  body:
    type: json
    data: |-
      {
        "name": "Updated Name"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
