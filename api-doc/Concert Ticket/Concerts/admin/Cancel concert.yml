info:
  name: Cancel concert
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/concerts/{{concertId}}/cancel"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
