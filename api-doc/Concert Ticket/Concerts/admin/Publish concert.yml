info:
  name: Publish concert
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/concerts/{{concertId}}/publish"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
