info:
  name: Check Availability
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/concerts/{{concertId}}/availability"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
