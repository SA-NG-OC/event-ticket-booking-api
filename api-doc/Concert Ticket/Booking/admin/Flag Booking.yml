info:
  name: Flag Booking
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/bookings/{{bookingId}}/flag"
  body:
    type: json
    data: |-
      {
          "reason": "Đơn hàng có dấu hiệu bất thường, mua số lượng lớn liên tục"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
