info:
  name: Get concert detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/concerts/{{concertId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
